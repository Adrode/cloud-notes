#decorators
- **Decorator** - funkcja, która rozszerza funkcjonalność innej funkcji
	- nie modyfikuje bazowej funkcji
	- do decorator w argumencie podaje się bazową funkcję
	- ```python
	  def add_sprinkles(func): # to jest kod decoratora
		  def wrapper(*args, **kwargs):
			  print("*You add sprinkles*") # to jest dodatkowy kod decoratora
			  func(*args, **kwargs)
			  # to jest kod przekazanej w argumencie funkcji
		  return wrapper # BEZ NAWIASÓW () !!!
		  
	  def add_fudge(func):
		  def wrapper(*args, **kwargs)
			  print("*You add fudge*")
			  func(*args, **kwargs)
		  return wrapper
		  
	  @add_sprinkles
	  @add_fudge			# to są decoratory
	  def get_ice_cream(flavor):
		  print(f"Here is your {flavor} ice cream 🍦")
		  
	  get_ice_cream("chocolate")
	  ```
		- **`wrapper`** jest tutaj po to, aby móc zwrócić kod przekazanej do decoratora funkcji (**sam kod, nie wynik funkcji**, dlatego return jest bez nawiasów)
			- **`wrapper` jest szablonem, na bazie którego tworzy się decoratory**
		- teraz wywołanie bazowej funkcji `get_ice_cream()` powoduje wykonanie się kodu z decoratora, ale nie modyfikuje bazowego kodu
		- w sytuacji, gdyby funkcja bazowa przyjmowała jakiekolwiek arugmenty, do wrapperów w decoratorach oraz do func trzeba przekazać w argumencie **`*args, **kwargs`** (jak w kodzie wyżej)
	- ```python
main = input("Type main ice cream flavor: ")
sprinkles = input("Type sprinkles flavor: ")
icing = input("Type icing flavor: ")

def add_sprinkles(sprinkles_flavor):
  def decorator(func):
    def wrapper(*args, **kwargs):
      print(f"*You add {sprinkles_flavor} sprinkles*")
      func(*args, **kwargs)
    return wrapper
  return decorator

def add_icing(icing_flavor):
  def decorator(func):
    def wrapper(*args, **kwargs):
      print(f"*You add {icing_flavor} icing*")
      func(*args, **kwargs)
    return wrapper
  return decorator

@add_sprinkles(sprinkles)
@add_icing(icing)
def get_ice_cream(main_flavor):
  print(f"Here is your {main_flavor} ice cream")

get_ice_cream(main)
	  ```
		- w tym przypadku, gdy **argumenty są przekazywane również do decorators**, należy zrobić to w powyższy sposób, czyli argumenty przekazywać do decorators
		- w innym wypadku, bez przekazywania ich bezpośrednio do decorators, tylko do bazowej funkcji, trzeba wtedy zadbać o kolejność przekazywania argumentów, na zasadzie:
			- `get_ice_cream()` jest obudowany przez `add_icing()`, które jest kolejno obudowane przez `add_sprinkles()`, więc do `get_ice_cream()` argumenty podajemy w kolejności - najpierw dla sprinkles, potem dla icing, potem dla bazowej, czyli:
				- `get_ice_cream(sprinkles, icing, main)`
					- jest to nieintuicyjne, więc dużo lepiej skorzystać z przekazania arugmentów do decorators